# Runtime dependencies for this AgentCore agent -- packaged and deployed
# via `agentcore deploy` (see this directory's README.md / DEPLOYMENT.md
# Section 7a). Pinned to recent versions rather than the exact minimum
# required.
bedrock-agentcore==1.18.1
strands-agents==1.48.0
